/**
 * ApprovedFeed — Static community photo gallery (frontend-only)
 */

export const ApprovedFeed = ({ spotId }: { spotId?: string }) => {
  // No photos available in frontend-only mode
  return null;
};
